import * as React from 'react';
import { Link } from 'react-router-dom';

const Page = () => {
  return (
    <React.Fragment>
      <Link to="/link-list">链表</Link>
    </React.Fragment>
  );
};

export default Page;
